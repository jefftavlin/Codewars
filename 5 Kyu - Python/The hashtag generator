def generate_hashtag(s):
    word = ''.join(word.title() for word in s.split())
    if len(word) < 140 and len(s) > 0:
        return '#' + word
    elif len(word) < 140:
        return False
    else:
        return False
