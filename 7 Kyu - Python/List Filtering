def filter_list(l):
  'return a new list with the strings filtered out'
  lst = []
  for item in l:
      try:
          if type(item) == type(7):
              lst.append(item)
      except ValueError:
          pass
  return lst
