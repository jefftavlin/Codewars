import numpy as np
def narcissistic( value ):
    lst = [int(item) for item in str(value)]
    array = np.array(lst)
    array = array ** len(array)
    if sum(array) == value:
        return True
    else:
        return False
